bugs ...
  python

to-do ...
  add line count
  find
  ctrl-c
  auto-copy on selection
  show banner positions in scrollbar
  use vscode colors in banner or steal colors from prism theme
  busy indicator

unlikely ...
  scroll body under banner?
  vue script shows as module (list.vue/311)
  persist multiple stacks
  show blocks with references to given def block

header config
  select theme
  scale (text size +-)

banner buttons ...
  close
  collapse/expand (1 block)

add configs ...
  does file text include path
  show entire files
  max lines output
  ignorePaths
  lang id mapping
  theme
  order of defs/refs

npm install -g @vscode/vsce
vsce package