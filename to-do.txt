bugs ...
  msg style broken
  doesn't show inital block reliably
  blocks after first, headers too tall

to-do ...
  wrap
  set max lines output
  use vscode colors in banner or steal colors from prism theme
  ignore empty blocks
  fix duplicates
  handle entire files (remove lines with no symbols)
  handle single-line defs (combine ones from same file)
  merge blocks that are immediately next to each other (e.g. toggleWaiting)
  show source when line num clicked
  highlight references
  navigate with clicks
  add configs
    ignorePath
    scale
    lang id mapping
    theme
    order of defs/refs
